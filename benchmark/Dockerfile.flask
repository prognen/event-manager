FROM python:3.13-slim

WORKDIR /app

RUN pip install --no-cache-dir flask psycopg2-binary bcrypt

COPY benchmark/flask_app/ /app/

EXPOSE 8000

CMD ["python", "-m", "flask", "--app", "app", "run", "--host", "0.0.0.0", "--port", "8000"]
